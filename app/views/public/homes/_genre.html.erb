
<%= form_with url: search_path, method: :get do |f| %>
  <table class="table table-borderless ml-2">
    <thead class="thead-light">
      <tr>
        <th>ジャンル</th>
        <th></th>
      </tr>
    </thead>
    
    <tbody>
      <% @genres.each do |genre| %>
        <tr>
          <td><%= link_to genre.name, search_path(genre_id: genre.id), method: :get %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>